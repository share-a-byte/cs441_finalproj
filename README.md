**To run**:
source env/bin/activate
cd video_clipping && python3 main.py

**video_clipping:**
- Contains all code for extracting chunks of audio from a song. Can modify interval length in the code. All resulting files stored in clips/ directory
